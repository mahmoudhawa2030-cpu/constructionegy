# Shared Components

Common UI components and utilities used by multiple modules.

## Components

### AvatarImage
Display user avatars with fallback to initials.

### VerifiedBadge
Show verification status badges.

## Usage

Copy the components you need to your app's components directory:

```bash
cp modules/shared/components/* your-app/components/
```

Make sure to import any required CSS variables for styling.
